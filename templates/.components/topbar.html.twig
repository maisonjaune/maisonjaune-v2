<nav class="navbar navbar-expand-xl topbar">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="{{ asset('build/images/logo.svg') }}" alt="La Maison Jaune" height="40px" />
        </a>
        <button class="navbar-toggler text-white" type="button" data-toggle="collapse" data-target="#topbar" aria-controls="topbar" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fa fa-bars" aria-hidden="true"></i>
        </button>

        <div class="collapse navbar-collapse" id="topbar">
            <ul class="navbar-nav">
                {# TODO Gestion du menu dans la topbar #}
            </ul>
            <ul class="navbar-nav ml-auto">
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {# TODO Affichage de l'avatar de l'utilisateur connecté sur la topbar #}
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="#">
                                {% trans %}My account{% endtrans %}
                            </a>
                            <a class="dropdown-item" href="#">
                                {% trans %}Logout{% endtrans %}
                            </a>
                        </div>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
